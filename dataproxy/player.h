#ifndef FRESHCITY_DATAPROXY_PLAYER
#define FRESHCITY_DATAPROXY_PLAYER

#include <string>

std::string CreateUser(const std::string& username, const std::string& password);

class User {
private:
	std::string _userid;

public:
	void SetAttribute(std::string key, std:;string value);
	std::

#endif
